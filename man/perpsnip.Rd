% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsnip-int.R
\name{perpsnip}
\alias{perpsnip}
\title{PerpetualBooster via parsnip}
\usage{
perpsnip(
  mode = "classification",
  engine = "perpetual",
  objective = NULL,
  budget = NULL,
  iteration_limit = NULL,
  stopping_rounds = NULL,
  max_bin = NULL,
  num_threads = NULL,
  missing = NULL,
  allow_missing_splits = NULL,
  create_missing_branch = NULL,
  missing_node_treatment = NULL,
  log_iterations = NULL,
  quantile = NULL,
  reset = NULL,
  timeout = NULL,
  memory_limit = NULL,
  seed = NULL,
  calibration_method = NULL,
  save_node_stats = NULL
)
}
\arguments{
\item{mode}{A string for the model mode. One of \code{"classification"},
\code{"regression"}, or \code{"censored regression"}. Defaults to
\code{"classification"}.}

\item{engine}{A string for the computational engine. Currently only
\code{"perpetual"}. Defaults to \code{"perpetual"}.}

\item{objective}{A string specifying the loss function.
\itemize{
\item Classification: \code{"LogLoss"} (default).
\item Regression: \code{"SquaredLoss"} (default).
\item Censored regression: \code{"SurvivalLogLikelihood"} (default).
}}

\item{budget}{A numeric value (0–1) controlling how much of the
self-generalization budget to spend. Higher values allow more iterations
and typically yield better models at the cost of longer training time.
\code{NULL} uses the algorithm's internal default.}

\item{iteration_limit}{An integer upper bound on the number of boosting
iterations. \code{NULL} means no explicit limit (budget governs stopping).}

\item{stopping_rounds}{An integer for early stopping: training halts if the
validation metric does not improve for this many consecutive rounds.
\code{NULL} disables early stopping.}

\item{max_bin}{An integer controlling the maximum number of histogram bins
used when discretising continuous features. Larger values may improve
accuracy but increase memory usage. \code{NULL} uses the default.}

\item{num_threads}{An integer specifying the number of parallel threads.
\code{NULL} uses all available cores.}

\item{missing}{The value to treat as missing data. Defaults to \code{NaN}
when \code{NULL}.}

\item{allow_missing_splits}{A logical. If \code{TRUE}, splits on features with
missing values are permitted.}

\item{create_missing_branch}{A logical. If \code{TRUE}, a dedicated branch is
created for missing values (producing ternary trees rather than binary
trees).}

\item{missing_node_treatment}{A string controlling how weights are assigned
to missing-value nodes when \code{create_missing_branch = TRUE}. One of
\code{"None"}, \code{"AssignToParent"}, \code{"AverageLeafWeight"}, or
\code{"AverageNodeWeight"}.}

\item{log_iterations}{An integer. If positive, training progress is printed
every this many iterations.}

\item{quantile}{A numeric value in (0, 1) used when \code{objective} is a
quantile-regression loss.}

\item{reset}{A logical. If \code{TRUE}, the booster is re-initialised before
fitting (relevant for incremental training).}

\item{timeout}{A numeric value (seconds). Training stops after this wall-
clock time even if \code{budget} or \code{iteration_limit} has not been reached.}

\item{memory_limit}{A numeric value (GB). Training stops if resident memory
usage exceeds this threshold.}

\item{seed}{An integer random seed for reproducibility.}

\item{calibration_method}{A string specifying the method used to generate
prediction intervals (\code{type = "interval"} in \code{predict()}). One of
\code{"WeightVariance"}, \code{"MinMax"}, \code{"GRP"}, or \code{"Conformal"}.}

\item{save_node_stats}{A logical. If \code{TRUE}, per-node statistics are stored
in the model object. Required for certain calibration methods and
importance types.}
}
\value{
A \code{model_spec} object of class \code{perpsnip}.
}
\description{
A parsnip-compatible model specification for the PerpetualBooster algorithm.
Perpetual is a self-generalizing gradient boosting machine that requires no
hyperparameter optimization — it automatically finds the best configuration
given a computational \code{budget}. Supports classification, regression, and
censored regression modes.
}
\examples{
\dontrun{
library(perpsnip)
library(workflows)

spec = perpsnip(mode = "classification", budget = 0.5)

wf = workflow() |>
  add_formula(am ~ .) |>
  add_model(spec) |>
  fit(data = dplyr::mutate(mtcars, am = as.factor(am)))
}

}
